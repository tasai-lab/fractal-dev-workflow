---
description: プラグインの状態を5カテゴリで評価しスコア付きレポートを生成
allowed-tools:
  - Read
  - Glob
  - Grep
  - Bash
  - Task
---

Invoke the fractal-dev-workflow:plugin-audit skill and follow it exactly as presented to you.

$ARGUMENTS があればオプションとして渡すこと（例: 特定カテゴリのみ評価）。

Use the Task tool to launch `fractal-dev-workflow:plugin-audit` with the provided arguments.
